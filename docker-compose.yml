version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: readout-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - GEMINI_API_KEY=${GEMINI_API_KEY}

